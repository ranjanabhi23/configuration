server:
  port: 8888

spring:
  application:
    name: auth-service

 datasource:
    url: jdbc:mysql://localhost:3306/order_auth
    username: root
    password: SqlKodewala25@
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
      database-platform: org.hibernate.dialect.MySQL8Dialect

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka
          register-with-eureka: true
    fetch-registry: true
